# 使用说明

## 文章草稿与隐藏规则

最近新增了文章可见性控制，主要有两个字段：

### `draft`

- `draft: true` 的文章：本地开发环境可见，正式构建/部署后隐藏
- `draft: false` 或不写：所有环境都正常展示

适用场景：
- 写到一半，想先在本地预览
- 不想让未完成内容出现在正式站点

### `hidden`

- `hidden: true` 的文章：开发和生产环境都隐藏
- 适合彻底不想公开展示的内容

## 怎么用

在文章的 Front Matter 里这样写：

```yaml
---
title: 示例文章
draft: true
date: 2026-05-06 12:00:00
---
```

如果想让文章完全不展示，改成：

```yaml
---
title: 示例文章
hidden: true
date: 2026-05-06 12:00:00
---
```

## 影响范围

设置后，下面这些地方会自动遵守规则：

- 文章列表
- 文章详情页
- 搜索
- 上一篇 / 下一篇
- 站点地图
- Atom 订阅
- 统计接口

## 精选文章功能

首页顶部的“精选文章”轮播，使用文章里的 `recommend` 字段控制。

### 基本写法

在文章的 Front Matter 里补上这些字段：

```yaml
---
title: 精选文章示例
description: 这是一篇会出现在首页精选区域的文章
date: 2026-05-06 12:00:00
image: /assets/example.jpg
recommend: 100
---
```

### 字段说明

- `recommend`：是否进入精选列表，以及精选排序优先级
- `image`：精选卡片封面图，建议提供
- `description`：精选卡片会用到的描述文字，建议提供

### 规则说明

- 文章需要位于 `content/posts/` 目录下
- `recommend` 只要不是空值，就会进入精选列表
- `recommend` 值越大，越靠前
- 当 `recommend` 相同时，会再参考文章日期排序
- 精选轮播只在首页第 1 页显示
- 如果当前按分类筛选文章，精选轮播不会显示

### 和草稿/隐藏的关系

- `draft: true`：本地开发时可以出现在精选里，正式部署后不会显示
- `hidden: true`：无论开发还是生产，都不会显示在精选里

## 文章侧栏 `aside`

`aside` 用来控制文章页右侧栏显示哪些内容块。

### 常见用法

```yaml
---
aside: [toc, meta-aside-foo, meta-aside-bar]
---
```

### 含义说明

- `toc`：显示文章目录
- `meta-aside-foo`：显示文章里定义的一个自定义侧栏插槽
- `meta-aside-bar`：显示文章里定义的另一个自定义侧栏插槽

### 自定义侧栏插槽示例

先在文章正文里定义插槽：

```mdc
::meta-aside-foo{title="补充说明" card}
这里是显示在文章侧栏中的内容。
::
```

然后在 Front Matter 里把它加到 `aside`：

```yaml
---
aside: [toc, meta-aside-foo]
---
```

这样页面右侧就会显示：

- 目录
- 这段自定义侧栏内容

### 使用建议

- 只想显示目录：`aside: [toc]`
- 想加文章专属补充信息：使用 `meta-aside-*`
- 如果不写 `aside`，文章页通常会默认显示目录

## 推荐用法

- **写作中**：用 `draft: true`
- **不公开**：用 `hidden: true`
- **想上首页精选**：加 `recommend: 数字`，最好同时提供 `image` 和 `description`
- **正式发布**：去掉 `draft`，不要设置 `hidden`
